# Amvera Cloud — Docker + persistent /data
# Docs: https://docs.amvera.ru/applications/configuration/docker.html

meta:
  environment: docker
  toolchain:
    name: docker

build:
  dockerfile: Dockerfile
  skip: false

run:
  persistenceMount: /data
  containerPort: "80"
